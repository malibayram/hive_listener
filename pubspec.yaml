name: hive_listener
description: A tiny widget to listen hive box changes
version: 0.0.1+2
repository: https://github.com/malibayram91/hive_listener

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^1.4.1+1
  hive_flutter: ^0.3.0+2
